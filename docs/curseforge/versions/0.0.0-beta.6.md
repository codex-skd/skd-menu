<h2>v0.0.0-beta.6 — Crash Fix</h2>

<h3>Fix</h3>
<ul>
<li><strong>Crash on Launch</strong>: Fixed mixin injection targeting <code>extractPanorama</code> which doesn't exist on <code>TitleScreen</code>. Now cancels <code>extractRenderState</code> directly instead. Mod no longer crashes at startup.</li>
</ul>
